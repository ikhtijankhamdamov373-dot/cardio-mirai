import { AcsTriageFlow } from "@/components/acs/AcsTriageFlow";

export const metadata = { title: "Emergency Cardiology — ACS / STEMI Triage | Cardio MIRAI" };

export default function AcsPage() {
  return <AcsTriageFlow />;
}
